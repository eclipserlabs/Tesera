[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tesera"
version = "0.2.0"
description = "Approval-gated, tamper-evident evidence for consequential Python function calls."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Wira Mahendra", email = "wiramahendra@proton.me" }]
requires-python = ">=3.10"
dependencies = [
    "cryptography>=42.0",
]
keywords = ["ai-agents", "approval", "audit", "evidence", "tesera", "ed25519"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
]

[project.urls]
Homepage = "https://github.com/eclipserlabs/tesera"
Changelog = "https://github.com/eclipserlabs/tesera/blob/main/CHANGELOG.md"
Documentation = "https://github.com/eclipserlabs/tesera"
"Threat Model" = "https://github.com/eclipserlabs/tesera/blob/main/docs/THREAT_MODEL.md"

[project.scripts]
tesera = "tesera.cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "hypothesis>=6.100",
    "mypy>=1.10",
    "ruff>=0.8",
]

[tool.hatch.build.targets.wheel]
packages = ["src/tesera"]

[tool.hatch.build.targets.sdist]
include = ["src/tesera", "docs", "README.md", "LICENSE", "SECURITY.md", "CHANGELOG.md"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=tesera --cov-report=term-missing --cov-fail-under=85"

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "S", "RUF"]
ignore = [
    "S101", # assert is used for invariants here, and throughout the tests
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S", "B011"]
# These files deliberately discuss Unicode lookalikes, so the ambiguous-glyph
# checks are noise here.
"src/tesera/canonical.py" = ["RUF002", "RUF003"]
"tests/test_redaction_homoglyphs.py" = ["RUF001", "RUF002", "RUF003"]
"tests/test_redaction_property.py" = ["RUF003"]

[tool.mypy]
python_version = "3.10"
strict = true
files = ["src/tesera"]
